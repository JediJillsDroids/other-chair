import { backend } from "./backend.js";
const view=document.getElementById("view"),status=document.getElementById("backendStatus"),languageToggle=document.getElementById("languageToggle");
const LANGUAGE_KEY="otherchair.baseline.language";let language=localStorage.getItem(LANGUAGE_KEY)||"en",route="home";
const copy={en:{languageButton:"한국어",homeTitle:"One plan. One empty chair.",homeText:"Choose the plan, not the profile.",humans:"Humans",humansText:"Offer a chair or take one.",games:"Games",gamesText:"Shared games for actual people in the same room.",back:"← Back",offer:"Offer a Chair",take:"Take a Chair"},ko:{languageButton:"English",homeTitle:"하나의 계획. 하나의 빈 의자.",homeText:"프로필이 아니라 계획을 고르세요.",humans:"사람들",humansText:"의자를 내어주거나 빈자리를 찾으세요.",games:"게임",gamesText:"같은 공간에 있는 사람들이 함께 하는 게임.",back:"← 뒤로",offer:"의자 내어주기",take:"의자 찾기"}};
const t=k=>copy[language][k]||copy.en[k]||k;const esc=v=>String(v??"").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[c]));
function setRoute(next){route=next;render();window.scrollTo({top:0,behavior:"smooth"});view.focus({preventScroll:true})}
function wireNavigation(){document.querySelectorAll("[data-route]").forEach(b=>b.onclick=()=>setRoute(b.dataset.route));document.querySelectorAll(".bottom-nav button").forEach(b=>b.classList.toggle("active",b.dataset.route===route))}
function render(){status.textContent=backend.statusLabel;languageToggle.textContent=t("languageButton");({home:renderHome,humans:renderHumans,offer:renderOffer,take:renderTake,games:renderGames,rules:renderRules,about:renderAbout,beacon:renderBeacon}[route]||renderHome)();wireNavigation()}
function renderHome(){view.innerHTML=`<section class="screen"><div class="hero"><span class="eyebrow">The Other Chair</span><h1>${t("homeTitle")}</h1><div class="deco-line"></div><p>${t("homeText")}</p></div><div class="choice-grid"><button class="choice-card teal" data-route="humans"><strong>${t("humans")}</strong><span>${t("humansText")}</span></button><button class="choice-card purple" data-route="games"><strong>${t("games")}</strong><span>${t("gamesText")}</span></button></div><div class="panel"><span class="eyebrow">Live baseline</span><h2>Plumbing before wallpaper.</h2><p>The shared Supabase backend is connected. Approved portrait artwork can replace these structural panels without disturbing the data layer.</p></div></section>`}
function renderHumans(){view.innerHTML=`<section class="screen"><button class="back" data-route="home">${t("back")}</button><div class="hero"><span class="eyebrow">Humans</span><h1>There’s room.</h1><p>Post the plan, not yourself.</p></div><div class="choice-grid"><button class="choice-card teal" data-route="offer"><strong>${t("offer")}</strong><span>You’re going anyway. Let somebody know there’s room.</span></button><button class="choice-card purple" data-route="take"><strong>${t("take")}</strong><span>Find a plan that sounds better with another human in it.</span></button></div><button class="btn ghost full" data-route="beacon">Open Beacon</button></section>`}
function renderOffer(){const now=new Date();now.setMinutes(now.getMinutes()-now.getTimezoneOffset());const minDate=now.toISOString().slice(0,10);view.innerHTML=`<section class="screen"><button class="back" data-route="humans">${t("back")}</button><div class="hero"><span class="eyebrow">Offer a Chair</span><h1>Post the plan,<br>not yourself.</h1><p>${backend.mode==="supabase"?"This plan will be shared through the Other Chair backend.":"Supabase is unavailable, so this device is temporarily using local fallback."}</p></div><form id="offerForm" class="form-shell"><label>Your first name or nickname</label><input name="host_name" maxlength="40" required placeholder="Jill"><label>What’s the plan?</label><input name="title" maxlength="80" required placeholder="Dinner and actual conversation"><label>What are you doing?</label><input name="activity" maxlength="50" required placeholder="Dinner, museum, movie..."><label>Place</label><input name="place" maxlength="120" required placeholder="Venue or meeting place"><div class="grid2"><div><label>City</label><input name="city" maxlength="60" placeholder="Seoul"></div><div><label>Area / neighborhood</label><input name="area" maxlength="60" placeholder="Cheongdam"></div></div><div class="grid2"><div><label>Date</label><input name="date" type="date" min="${minDate}" required></div><div><label>Time</label><input name="time" type="time" required></div></div><label>Open chairs</label><select name="seats">${Array.from({length:10},(_,i)=>`<option value="${i+1}">${i+1}</option>`).join("")}</select><label>One-line note</label><textarea name="description" maxlength="100" placeholder="I’m going either way. Join if this sounds good."></textarea><div id="offerMessage"></div><button class="btn teal full" id="offerButton" type="submit">Offer This Chair</button></form></section>`;document.getElementById("offerForm").onsubmit=async e=>{e.preventDefault();const button=document.getElementById("offerButton"),message=document.getElementById("offerMessage"),fd=new FormData(e.currentTarget),startsAt=new Date(`${fd.get("date")}T${fd.get("time")}`);button.disabled=true;button.textContent="Offering Chair…";message.innerHTML="";try{const result=await backend.offerChair({title:String(fd.get("title")).trim(),activity:String(fd.get("activity")).trim(),place:String(fd.get("place")).trim(),starts_at:startsAt.toISOString(),seats:Number(fd.get("seats")),description:String(fd.get("description")).trim(),host_name:String(fd.get("host_name")).trim(),city:String(fd.get("city")).trim(),area:String(fd.get("area")).trim()});message.innerHTML=`<div class="notice ok">✓ Chair offered${result.shared?" to the shared board":" on this device"}.</div>`;e.currentTarget.reset()}catch(error){console.error(error);message.innerHTML=`<div class="notice error">${esc(error.message||"Could not offer the chair.")}</div>`}finally{button.disabled=false;button.textContent="Offer This Chair"}}}
function renderTake(){view.innerHTML=`<section class="screen"><button class="back" data-route="humans">${t("back")}</button><div class="hero"><span class="eyebrow">Take a Chair</span><h1>Find a plan.</h1><p>${backend.mode==="supabase"?"Shared chairs from the Other Chair backend.":"Local fallback chairs on this device."}</p></div><div class="form-shell"><label>Search</label><input id="chairSearch" placeholder="Cheongdam, museum, dinner..."><div class="button-row"><button class="btn teal" id="searchButton" type="button">Find Chairs</button><button class="btn ghost" id="allButton" type="button">Show All</button></div></div><div id="chairMessage"></div><div id="chairList" class="stack"></div></section>`;const paint=async term=>{const holder=document.getElementById("chairList"),note=document.getElementById("chairMessage");holder.innerHTML="";note.innerHTML='<div class="notice">Looking for chairs…</div>';try{const chairs=await backend.findChairs(term);note.innerHTML="";if(!chairs.length){holder.innerHTML='<div class="notice">No chairs match yet.</div>';return}holder.innerHTML=chairs.map(c=>`<article class="chair-card"><h3>${esc(c.title||c.activity||"Open Chair")}</h3><div class="meta">${esc(c.activity||"Plan")} · ${new Date(c.starts_at).toLocaleString()}</div><p><strong>${esc(c.place)}</strong>${c.area?" · "+esc(c.area):""}${c.city?" · "+esc(c.city):""}</p>${c.description?`<p>${esc(c.description)}</p>`:""}<div class="tiny">Offered by ${esc(c.host_name||"a human")} · ${Number(c.seats_open??c.seats)} open</div>${c.is_mine?'<span class="mine">This one is yours.</span>':`<button class="btn small requestButton" data-id="${c.id}" type="button">Take This Chair</button>`}</article>`).join("");document.querySelectorAll(".requestButton").forEach(button=>{button.onclick=async()=>{button.disabled=true;const original=button.textContent;button.textContent="Taking Chair…";try{await backend.takeChair(button.dataset.id);button.textContent="✓ Requested"}catch(error){console.error(error);button.disabled=false;button.textContent=original;alert(error.message||"Could not take this chair.")}}})}catch(error){console.error(error);note.innerHTML=`<div class="notice error">${esc(error.message||"Could not load chairs.")}</div>`}};document.getElementById("searchButton").onclick=()=>paint(document.getElementById("chairSearch").value);document.getElementById("allButton").onclick=()=>paint("");paint("")}
function renderGames(){const games=[["What a Character!","Typecast and Know Thy Neighbor. Approved visual direction: teal."],["You Talk Too Much","Comedy or Tragedy. Approved visual direction: purple."],["Rec Room","Shared team setup, shared scoring, and quick group games."],["The Looking Glass","Map, Looking Glass, Evidence, and Scores."],["Spin Cycle","Spin, do the thing, pass it on."]];view.innerHTML=`<section class="screen"><button class="back" data-route="home">${t("back")}</button><div class="hero"><span class="eyebrow">Games</span><h1>Same humans.<br>Different trouble.</h1><p>These are structural placeholders for the approved portrait artwork, not redesigns.</p></div><div class="game-grid">${games.map(([n,d])=>`<button class="game-card" type="button"><strong>${n}</strong><span>${d}</span></button>`).join("")}</div></section>`}
function renderRules(){view.innerHTML=`<section class="screen"><div class="hero"><span class="eyebrow">Rules</span><h1>Be a decent human.</h1><p>Other Chair is for meeting around real plans and shared activities, not judging profiles.</p></div><div class="panel"><h3>Core rules</h3><p>Public places first. Respect a no. Do not post other people’s private information. Games are optional. Leaving is always allowed.</p></div></section>`}
function renderAbout(){view.innerHTML=`<section class="screen"><div class="hero"><span class="eyebrow">About</span><h1>I’m going anyway.</h1><p>Other Chair makes it easier to go somewhere alone without requiring you to stay alone.</p></div><div class="panel"><h2>The point</h2><p>No personality contest. No beauty contest. No swiping. Post the thing you’re doing, leave a chair open, and let another human decide whether the plan sounds good.</p></div></section>`}
function renderBeacon(){view.innerHTML=`<section class="screen"><button class="back" data-route="humans">${t("back")}</button><div class="beacon"><div><span class="eyebrow">Beacon</span><strong>OTHER<br>CHAIR</strong><p>This is the chair you’re looking for.</p></div></div></section>`}
languageToggle.onclick=()=>{language=language==="en"?"ko":"en";localStorage.setItem(LANGUAGE_KEY,language);render()};await backend.init();render();
